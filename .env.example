# ===== Application =====
# Environment: production | development | test
ENV=development
DEBUG=true

# Port for FastAPI (if using uvicorn directly); Gunicorn bind takes precedence in prod
PORT=8000

# Admin API key for /api/admin/* routes (set a strong secret in production)
API_KEY=change-me

# ===== Redis =====
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# ===== Model registry =====
# Max number of different models kept resident in memory (avoid OOM on GPU)
MODEL_CAPACITY=1

# ===== Caching =====
# Default TTL in seconds for caption cache entries
CACHE_TTL_SECONDS=86400